# 状态字典同步更新计划

## 一、背景分析

基于当前状态关联字典（01-草稿态，02-已提请，03-审批中，04-已合并，05-已部署，06-测试中，07-已评审，08-待投产，09-已投产），需要更新相关文件和状态管理功能。

## 二、当前状态分析

### 1. 前端组件状态映射
- **ChangeRecordList.vue** 和 **ChangeRecordHistory.vue** 中的 `statusTagType` 函数仅支持4种状态（01-04）
- 状态标签显示与新字典不一致（如01显示"待审批"而非"草稿态"）
- 缺少对05-09状态的支持

### 2. 表单状态选择
- **ChangeRecordForm.vue** 中的状态选择使用了字典项，但需要确保与新字典一致

### 3. 后端实体类
- 需要检查 **ChangeRecord.java** 中的状态字段定义

## 三、更新计划

### 1. 实体类更新
- **文件**：`src/main/java/com/aiguibin/platform/arch/entity/ChangeRecord.java`
- **内容**：更新状态字段注释，确保与新字典一致

### 2. 前端状态映射更新
- **文件**：`ChangeRecordList.vue` 和 `ChangeRecordHistory.vue` 中的 `statusTagType` 函数
- **内容**：
  - 更新状态值映射关系
  - 添加05-09状态支持
  - 确保状态标签显示正确

### 3. 表单状态选择更新
- **文件**：`ChangeRecordForm.vue`
- **内容**：确保状态选择下拉框与新字典一致

### 4. 状态标签显示更新
- **文件**：`ChangeRecordList.vue` 和 `ChangeRecordHistory.vue` 中的状态显示模板
- **内容**：确保状态标签显示新字典中的状态名称

## 四、详细实施步骤

### 步骤1：更新实体类状态字段
- 检查并更新 `ChangeRecord.java` 中的状态字段定义和注释

### 步骤2：更新前端状态映射函数
```javascript
// 更新 statusTagType 函数，支持所有9种状态
statusTagType(s) {
  switch (s) {
    case '01': return 'warning'; // 草稿态
    case '02': return 'info';    // 已提请
    case '03': return 'primary'; // 审批中
    case '04': return 'success'; // 已合并
    case '05': return 'success'; // 已部署
    case '06': return 'info';    // 测试中
    case '07': return 'success'; // 已评审
    case '08': return 'warning'; // 待投产
    case '09': return 'success'; // 已投产
    default: return '';
  }
}
```

### 步骤3：更新状态标签显示
- 将状态标签显示从直接显示状态值改为显示对应字典名称

### 步骤4：确保表单状态选择正确
- 检查并更新 `ChangeRecordForm.vue` 中的状态选择选项

### 步骤5：验证更新结果
- 运行构建命令确保无编译错误
- 测试状态显示和选择功能

## 五、预期效果

1. 所有状态值与新字典保持一致
2. 前端显示正确的状态标签和样式
3. 表单状态选择支持所有新字典中的状态
4. 后端实体类注释与新字典一致
5. 无编译错误和功能异常