## 修正相关文件以匹配最新的ChangeRecord实体类

### 需求分析
根据最新的ChangeRecord实体类，需要修正相关依赖文件，确保它们使用正确的字段名和类型。主要修改包括：
1. 将`branchName`替换为`sourceBranch`和`targetBranch`
2. 将`solution`替换为`solutionDescription`
3. 添加其他新字段的支持

### 实现步骤
1. **修正ChangeRecordService.java**：
   - 在`saveHistory`方法中，将`branchName`替换为`sourceBranch`和`targetBranch`
   - 将`solution`替换为`solutionDescription`

2. **修正CodeScriptChangeExportController.java**：
   - 将`branchName`替换为`sourceBranch`和`targetBranch`
   - 将`solution`替换为`solutionDescription`
   - 更新Excel标题和数据行

3. **修正ExcelEntity.java**：
   - 将`branchName`替换为`sourceBranch`和`targetBranch`
   - 将`solution`替换为`solutionDescription`
   - 添加新字段：`includeShell`、`shellPath`、`configList`等

4. **修正ApprovalServiceImpl.java**：
   - 在`saveHistory`方法中，将`branchName`替换为`sourceBranch`和`targetBranch`
   - 将`solution`替换为`solutionDescription`
   - 添加新字段的支持

### 预期效果
修正后的文件将与最新的ChangeRecord实体类完全匹配，确保ORM映射正确，数据传输和导出功能正常工作。

### 修改文件
1. `e:\WorkSpace\HelloWorldBackend\aiguibin-platform-arch\src\main\java\com\aiguibin\platform\arch\service\ChangeRecordService.java`
2. `e:\WorkSpace\HelloWorldBackend\aiguibin-platform-arch\src\main\java\com\aiguibin\platform\arch\controller\CodeScriptChangeExportController.java`
3. `e:\WorkSpace\HelloWorldBackend\aiguibin-platform-arch\src\main\java\com\aiguibin\platform\arch\excel\ExcelEntity.java`
4. `e:\WorkSpace\HelloWorldBackend\aiguibin-platform-arch\src\main\java\com\aiguibin\platform\arch\service\impl\ApprovalServiceImpl.java`