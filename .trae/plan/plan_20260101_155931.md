## 修正ChangeHistory实体类

### 需求分析
根据提供的`biz_change_history`表结构，需要修正`ChangeHistory.java`实体类，确保字段名、类型和注释与数据库表完全匹配。

### 实现步骤
1. **新增缺失字段**：
   - `sourceBranch`：源分支名称
   - `targetBranch`：目标分支名称
   - `includeShell`：是否包含脚本
   - `shellPath`：脚本清单
   - `configList`：配置说明
   - `flowId`：关联流程ID
   - `currentNodeId`：当前节点ID
   - `approvalInstanceId`：审批实例ID
   - `approvalStatus`：审批状态
   - `submitTime`：提交审批时间
   - `rejectReason`：拒绝原因
   - `rejectNodeId`：拒绝节点ID

2. **修改字段名**：
   - 将`solution`改为`solutionDescription`，以匹配表字段`solution_description`

3. **删除多余字段**：
   - 删除`branchName`字段，因为表中没有这个字段

4. **更新注释**：
   - 更新`currentStatus`的注释，添加具体的状态列表
   - 更新`releaseDate`的注释，从"发布日期"改为"提请投产日期"
   - 更新`defectNumber`的注释，从"缺陷编号"改为"问题编号"
   - 更新`groupName`的注释，从"组别"改为"模块组别"
   - 更新`developType`的注释，添加具体的开发类别列表
   - 更新其他字段的注释，确保与表结构匹配

5. **验证修改**：
   - 确保所有字段名使用驼峰命名
   - 确保所有字段类型与表结构匹配
   - 确保所有注释准确反映字段含义

### 预期效果
修正后的`ChangeHistory.java`实体类将与`biz_change_history`表结构完全匹配，包括所有字段、类型和注释，确保ORM映射正确，便于后续的数据操作。

### 修改文件
- `e:\WorkSpace\HelloWorldBackend\aiguibin-platform-arch\src\main\java\com\aiguibin\platform\arch\entity\ChangeHistory.java`