## README表结构核对与功能检核修正计划

### 1. 项目结构修正
- **问题**：README中项目结构显示的项目名称为`aiguibin-online-table`，但实际项目名称为`aiguibin-platform-arch`
- **修正**：将项目结构中的项目名称统一改为`aiguibin-platform-arch`

### 2. 权限分类体系示例修正
- **问题**：权限分类体系部分引用了`sys_data_scope`和`time_permission`等表名，但实际表结构中对应的表名为`sys_data_rule`和`sys_time_permission`
- **修正**：将示例中的表名替换为实际表名，确保与当前表结构一致

### 3. 功能描述与实际表结构匹配
- **问题**：部分功能描述中的示例字段与实际表结构有差异
- **修正**：调整功能描述中的示例，使其与实际表结构完全匹配

### 4. 表结构与功能覆盖完整性确认
- **确认**：所有表的字段设计合理，覆盖了RBAC权限管理、机构部门管理、变更记录管理等核心功能
- **确认**：表间关系清晰，外键关联合理
- **确认**：字段命名规范统一，注释完整

### 5. 构建与运行命令修正
- **问题**：构建命令中的输出jar包名称与实际项目名称不符
- **修正**：将jar包名称从`aiguibin-online-table.jar`改为`aiguibin-platform-arch.jar`

### 6. 权限示例代码修正
- **问题**：权限示例代码中的注解和权限标识与实际实现可能存在差异
- **修正**：调整示例代码，使其与当前的权限管理设计保持一致

### 7. 字段权限示例修正
- **问题**：字段权限示例中的代码与实际表结构不完全匹配
- **修正**：更新字段权限示例，使用实际表结构中的字段名和实体类型

### 8. 时间权限示例修正
- **问题**：时间权限示例中的表名与实际表结构不符
- **修正**：将示例中的表名改为`sys_time_permission`，并调整字段名以匹配实际表结构

通过以上修正，确保README文件中的表结构描述、功能说明和示例代码与实际项目完全一致，提高文档的准确性和可用性。