# 前端视图文件同步修正计划

## 1. 分析实体类变化

根据最新的`ChangeRecord.java`实体类定义，主要变化包括：
- `branchName` → 拆分为 `sourceBranch` 和 `targetBranch`
- `solution` → 改为 `solutionDescription`
- 新增 `includeShell`、`shellPath`、`configList` 字段
- `developer` → 拆分为 `developerNum` 和 `developerName`
- `createTime` → `createdTime`，`updateTime` → `updatedTime`

## 2. 修改计划

### 2.1 ChangeRecordDetail.vue

**修改内容：**
- 将 `detail.developer` 改为 `detail.developerName`
- 将 `detail.createTime` 改为 `detail.createdTime`
- 将 `detail.updateTime` 改为 `detail.updatedTime`
- 将 `detail.solution` 改为 `detail.solutionDescription`
- 新增 `sourceBranch`、`targetBranch`、`includeShell`、`shellPath`、`configList` 等字段的展示

### 2.2 ChangeRecordForm.vue

**修改内容：**
- 将 `branchName` 字段替换为 `sourceBranch` 和 `targetBranch`
- 将 `developer` 字段替换为 `developerNum` 和 `developerName`
- 将 `solution` 字段改为 `solutionDescription`
- 新增 `includeShell`、`shellPath`、`configList` 等字段的表单控件
- 更新验证规则，将 `branchName` 和 `solution` 替换为新字段

### 2.3 ChangeRecordList.vue

**修改内容：**
- 将搜索栏中的 `search.developer` 替换为 `search.developerName`
- 将表格中的 `developer` 字段改为 `developerName`
- 将表格中的 `branchName` 字段替换为 `sourceBranch` 和 `targetBranch`
- 将表格中的 `solution` 字段改为 `solutionDescription`
- 新增 `includeShell`、`shellPath`、`configList` 等字段的表格列
- 更新 form 初始值，将 `branchName` 和 `solution` 替换为新字段
- 更新 openCreate 方法，将 `branchName` 和 `solution` 替换为新字段
- 更新 CSV 导出的表头和数据行，将 `branchName` 和 `solution` 替换为新字段

## 3. 实施步骤

1. 首先修改 `ChangeRecordForm.vue`，确保表单字段与实体类一致
2. 然后修改 `ChangeRecordList.vue`，确保列表展示和搜索功能正常
3. 最后修改 `ChangeRecordDetail.vue`，确保详情页展示完整信息
4. 运行 `npm run build` 验证修改后的代码可以正常构建
5. 测试页面渲染、数据提交和列表展示功能

## 4. 注意事项

- 严格保留原有业务逻辑和用户交互功能
- 不引入新的功能变更
- 确保所有数据绑定、表单字段、列表展示项与实体类保持一致
- 验证修改后的功能正常运行，无数据不匹配或功能异常问题